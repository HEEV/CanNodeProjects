To use the CSV parser
be sure that your input csv is in ascending order
use the spreadsheet and adjust the curved portion as neccessary
copy the throttleVal Min->Max onto the left side of the csv and the throttleVal Ints. Min->Max onto the right side 
run the code in the folder
copy the .h into the file path of the main and include it in the directory

